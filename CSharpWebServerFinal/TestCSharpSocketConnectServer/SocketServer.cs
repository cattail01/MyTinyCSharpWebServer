namespace TestCSharpSocketConnectServer;

/// <summary>
/// 一个socket 连接的 server端
/// </summary>
class SocketServer
{
    public static void Main()
    {
        Console.WriteLine("socket server is running!");
        // 1. 创建socket
        // Socket(AddressFamily, SocketType, ProtocolType)
        
    }
}